import { useContext } from "react"
import { useState } from "react"


export const Skills = () => {

    //const [skills, setSkills] = useState(['PHP', 'Ruby', 'Java Script'])

    //const { skills, addSkill, removeSkill } = useContext(SkillsContext)

    //addSkill('asd')
    //console.log(value)

    return (
        <div>
            {
                //skills.map(skill => <div>{skill}</div>)
            }
        </div>
    )
}